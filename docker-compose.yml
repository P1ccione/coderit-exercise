version: '3.7'
services:
  frontend:
    container_name: vue-app-container
    build: .
    ports:
      - '8080:80'

  backend:
    container_name: json-server-container
    build: ./db
    ports:
      - '5000:5000'